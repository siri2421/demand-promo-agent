[project]
name = "weather-mcp-cr"
version = "0.1.0"
description = "MCP Server that gets weather form the NWS API"
requires-python = ">=3.10"
dependencies = [
    "fastmcp==2.8.0",
    "requests==2.32.5",
]